# Test mTLS Doc App

Some intro text that must not leak into the addon notice.

## addon-mtls

MANUAL-MTLS-FIXTURE-MARKER: this app is not auto-configured; wire the certs
from `/certs/mtls/<hostname>/` into your flow manually.

## Other

Unrelated section that must not be returned.
