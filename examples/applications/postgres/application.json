{
  "name": "Postgres",
  "description": "PostgreSQL (often shortened to Postgres) is a powerful, open-source object-relational database system. With over 35 years of active development, it has earned a strong reputation for reliability, feature robustness, and performance. ",
  "extends": "oci-image",
  "icon": "Postgresql_elephant.svg",
  "parameters": [
    {
      "id": "hostname",
      "name": "Hostname",
      "required": false,
      "type": "string",
      "description": "Hostname for the LXC container",
      "template": "100-conf-create-configure-lxc.json",
      "templatename": "Create and Configure LXC",
      "default": "postgres"
    },
    {
      "id": "volumes",
      "name": "Volumes",
      "type": "string",
      "multiline": true,
      "required": false,
      "description": "One per line: key=/container/path",
      "template": "150-conf-create-storage-volumes-for-lxc.json",
      "templatename": "Create Storage Volumes for LXC",
      "default": "data:/var/lib/postgresql/data"
    },
    {
      "id": "envs",
      "name": "Environment Variables",
      "type": "string",
      "advanced": true,
      "multiline": true,
      "description": "Environment variables in key=value format, one per line. Each line sets an environment variable in the container configuration. Example:\nMYAPP_HOST=localhost\nMYAPP_PORT=8080\nDATABASE_URL=postgresql://localhost:5432/mydb",
      "template": "170-conf-set-environment-variables-in-lxc.json",
      "templatename": "Env Variables in LXC",
      "default": "GID=1000\nPGDATA=/var/lib/postgresql/data/pgdata\nPOSTGRES_DB=postgres\nPOSTGRES_PASSWORD={{ POSTGRES_PASSWORD }}\nPOSTGRES_PORT=5432\nPOSTGRES_USER=postgres\nPOSTGRES_VERSION=16-alpine\nUID=1000"
    }
  ],
  "properties": [
    {
      "id": "oci_image",
      "value": "postgres:16-alpine"
    },
    {
      "id": "uid",
      "value": "1000"
    },
    {
      "id": "username",
      "value": "root"
    }
  ],
  "installation": [],
  "tags": [
    "database"
  ]
}