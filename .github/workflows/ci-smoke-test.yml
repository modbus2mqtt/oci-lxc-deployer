name: CI Smoke Test

on:
  workflow_dispatch:

jobs:
  smoke-test:
    name: Runner smoke test
    runs-on: [self-hosted, linux, pve1]
    timeout-minutes: 10
    steps:
      - name: Wake backend
        run: pvetest wake

      - name: Check infrastructure
        run: pvetest check
