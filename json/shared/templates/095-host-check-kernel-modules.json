{
  "execute_on": "ve",
  "name": "Check Kernel Modules",
  "description": "Check if required kernel modules for Docker are loaded on the Proxmox host. Fails if modules are not loaded.",
  "parameters": [],
  "commands": [
    {
      "script": "host-check-kernel-modules.sh",
      "description": "Check if overlay, ip_tables, ip6_tables, bridge, and br_netfilter modules are loaded"
    }
  ]
}