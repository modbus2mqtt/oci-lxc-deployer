{
  "execute_on": "ve",
  "name": "Load Kernel Modules",
  "description": "Load required kernel modules for Docker on the Proxmox host. Modules are loaded temporarily (until reboot). For persistent loading, add them to /etc/modules-load.d/docker.conf",
  "parameters": [],
  "commands": [
    {
      "script": "host-load-kernel-modules.sh",
      "description": "Load overlay, ip_tables, ip6_tables, bridge, and br_netfilter modules"
    }
  ]
}