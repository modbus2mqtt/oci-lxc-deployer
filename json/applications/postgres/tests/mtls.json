{
  "selectedAddons": ["addon-ssl"],
  "params": [
    { "name": "pg_client_cert", "value": "true" }
  ],
  "wait_seconds": 30,
  "description": "addon-ssl + pg_client_cert=true — PostgreSQL requires CA-signed client certs (managed pg_hba.conf cert-only, ssl_ca_file=/certs/chain.pem); password-only TCP rejected"
}
