{
  "selectedAddons": ["addon-ssl", "addon-mtls"],
  "params": [
    { "name": "upload_mosquitto_configuration_content", "value": "file:mosquitto-mtls.conf" },
    { "name": "mtls_cns", "value": "{{ hostname }}\nalice\nbob" }
  ],
  "wait_seconds": 30,
  "description": "addon-ssl (broker server cert) + addon-mtls with multiple CNs — issues CA-signed client certs for the hostname plus alice + bob into the certs-volume mtls/ subdir (/mosquitto/config/certs/mtls/<cn>/). The default mtls.check_file_paths verifies the hostname cert; alice/bob exercise multi-CN issuance."
}
