{
  "name": "Assert app schema in PgREST",
  "description": "Iteration 1: fail-loud if the postgrest container's PGRST_DB_SCHEMAS does not include <slug>_api. The operator must reconfigure postgrest with the extended db_schemas before installing this app. Iteration 2 (separate PR) will replace this with an automatic schema-feedback mechanism.",
  "execute_on": "ve",
  "parameters": [
    "app_slug",
    "POSTGREST_HOST"
  ],
  "commands": [
    {
      "name": "Assert schema",
      "script": "assert-postgrest-schema.sh"
    }
  ]
}
